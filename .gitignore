.DS_Store
.build/
build/
DerivedData/
xcuserdata/
*.xcuserstate
*.metallib
*.air
*.trace

# Local Numi Lab execution evidence is retained locally, not committed by default.
# Selected compact evidence belongs under evidence/ after explicit review.
.numi/

# Long-lived agent state and training artifacts require explicit publication.
checkpoints/
datasets/
artifacts/
